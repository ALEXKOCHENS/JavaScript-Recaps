## Below are the concepts i grasped from this section!

# whats JavaScript:

--JavaScript is a scripting language that enables developers create dynamically updating contents, control multimedia, animate images and pretty much more.

# console

...its a text based interface that can be used to run commands on the computer.

# log function()

..this is simply a piece fo code that is used to perform a certain task, it prints whatever is put inside the parenthesis, in contrast a function is a piece of code that performs a specific task when invoked.

--How to output different values in the browser console, including a table, variable values, numbers and boolean
--applying a css simple styling from the browser console using the prefix of [%c], followed by the argument passed as a variable holding the stylings.
--outputting an error message from the browser using console.error() method.
--outputting a warning message using console.warn() method.
--Also learnt multiple IDE(Integrated Development Environment specifically VS code short cuts for x2 development)

## The JS Code structure.

...A computer program is a series of pieces of codes written in a text file. These text files are then run through a software that is designed to run the codes. Node.js for Javascript

## A statement

...This is a single instruction for the computer to run.its just like a statement but in computing we can terminate them using a semi colon[ ; ]

# The execution flow

..the Javascript statements get executed using the top down approach. The first line of statement gets executed first, followed by the second line, way through the last line.

## Diving in to the Javascript variables next::
